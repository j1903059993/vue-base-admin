# Themes

Semantic colors, shape, depth, typography, and motion live in `src/styles/tokens.css`. Components reference these tokens instead of hard-coded surface colors.

The settings store persists light, dark, or system mode, the brand color, interface density, and sidebar state. Add semantic tokens when a value is reused across components; reserve success, warning, danger, and info colors for their meanings.
